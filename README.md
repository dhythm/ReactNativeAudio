# Installation
```bash
npx react-native init ReactNativeAudio --template react-native-template-typescript

yarn add react-native-audio
yarn add -D @types/react-native-audio
# Add a usage description to Info.plist
cd ios && pod install && cd ..
```